---
description: Ingest a supplied source into an OpenViking wiki.
argument-hint: <source and optional private|shared scope>
---

Use the `openviking-wiki` skill and its ingest and write/recovery references for `$ARGUMENTS`.
Discover the registered OpenViking tools in this Claude Code session; if required read/search/write
capabilities are unavailable, stop with the missing capability instead of guessing a tool name.
