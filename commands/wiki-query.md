---
description: Answer a question from an OpenViking wiki with page citations.
argument-hint: <question and optional scope>
---

Use the `openviking-wiki` skill's query workflow for `$ARGUMENTS`. Keep the operation read-only unless
the user explicitly asks to file the result. Discover the registered OpenViking tools at runtime.
